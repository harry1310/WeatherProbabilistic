pymc>=5.25
arviz>=0.23
pandas>=2.0
pyarrow>=15
numpy>=1.26
scikit-learn>=1.4
scipy>=1.11
matplotlib>=3.8
# Rust-based NUTS sampler (used because g++ is not installed,
# so PyTensor's C backend is unavailable). Requires numba at runtime.
nutpie>=0.16
numba>=0.60
# DuckDB powers the schema-tolerant parquet reader used by predict_4a /
# predict_5a / the bake-off scripts (hive_partitioning + union_by_name).
duckdb>=1.0
# predict_4a / predict_5a transitively import scripts/run_phase6_bart_bakeoff.py
# (resolve_station / build_features_via_duckdb / FEATURE_NAMES / time_split),
# whose top-level imports include both lightgbm (per-lead 3a/3c/3d champion
# baselines) and pymc_bart (the original Python-side BART path retained for
# bake-off comparison). Both are needed at import time even though predict_4a
# uses dbarts via rpy2 instead.
lightgbm>=4.0
pymc-bart>=0.5
